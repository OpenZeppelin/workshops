keys:
  api: <Defender API Key goes here>
  secret: <Defender Secret goes here>

# WARNING: When deploying stacks, the secrets listed below will overwrite the ones in Defender.
# If secrets do not need to be updated, comment them out in your serverless.yml files.
# Note: The overwrite is limited to the stack that you are deploying.
secrets:
  alertEmail: <Your@email.com>
